# Auto detect text files and perform LF normalization
* text=auto
# Shell scripts and Docker files must stay LF or they break inside the Linux image
*.sh        text eol=lf
Dockerfile  text eol=lf
*.yml       text eol=lf
*.json      text eol=lf
